-- ================================================================
-- pay_wallet 表乐观锁字段迁移
-- 作者：Claude
-- 日期：2026-04-11
-- 描述：P0-3 并发扣减安全（乐观锁）
-- ================================================================

-- 给 pay_wallet 表添加 version 乐观锁字段
ALTER TABLE `pay_wallet` ADD COLUMN `version` INT DEFAULT 0 COMMENT '乐观锁版本号' AFTER `expire_time`;

-- 给已存在的钱包记录设置初始版本号（从0开始）
-- UPDATE `pay_wallet` SET `version` = 0 WHERE `version` IS NULL;

-- ================================================================
-- 说明：
-- 乐观锁工作原理：
-- 1. 每次扣减时 WHERE 条件包含 version = 当前版本号
-- 2. 扣减成功时 version + 1
-- 3. 如果 version 被其他事务更新，则 WHERE 条件不匹配，扣减失败
-- 4. 调用方捕获异常后可以重试（建议最多重试3次）
-- ================================================================
